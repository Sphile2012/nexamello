// Phume API client
// This is a static marketing site — no backend API required.
// Add fetch calls here if you need a backend in the future.
// e.g. Netlify Functions at /.netlify/functions/<name>

export const phumeClient = {
  // Example: submit contact form via Netlify Function
  // submitContact: async (data) => {
  //   const res = await fetch('/.netlify/functions/contact', {
  //     method: 'POST',
  //     headers: { 'Content-Type': 'application/json' },
  //     body: JSON.stringify(data),
  //   });
  //   return res.json();
  // },
};
